spring:
  datasource:
    url: jdbc:log4jdbc:postgresql://localhost:5432/pms
    username: seg00
    password: seg00
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
    generate-ddl: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 100
          order_inserts: true
          order_updates: true
  data:
    redis:
      url: localhost
      port: 6379

logging:
  level:
    org:
      springframework: debug